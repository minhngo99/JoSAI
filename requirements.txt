# AI from Scratch - Requirements
# Python 3.10+ required (for M1 Mac compatibility)

# Core dependencies
numpy>=1.24.0
torch>=2.0.0  # M1 native support with MPS

# Optional but recommended
jupyter>=1.0.0
matplotlib>=3.7.0
tqdm>=4.65.0

# For later (when we build models)
# scikit-learn>=1.3.0
# pandas>=2.0.0
